apiVersion: apps/v1
kind: Deployment
metadata:
  name: one8pulse-sosservice
  namespace: one8pulse
spec:
  replicas: 2
  selector:
    matchLabels:
      app: one8pulse
      component: sosservice
  template:
    metadata:
      labels:
        app: one8pulse
        component: sosservice
    spec:
      containers:
      - name: one8pulse-sosservice
        image: 797111435256.dkr.ecr.us-east-1.amazonaws.com/backend/sosservice:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8089
        env:
          - name: SPRING_CONFIG_IMPORT
            value: "optional:configserver:http://one8pulse-configserver-service.one8pulse.svc.cluster.local:8888"
        envFrom:
          - secretRef:
              name: one8pulse-secrets
        resources:
          requests:
            cpu: 200m
            memory: 384Mi
          limits:
            cpu: 500m
            memory: 512Mi
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8089
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8089
          initialDelaySeconds: 15
          periodSeconds: 20
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: one8pulse-sos-service
  namespace: one8pulse
spec:
  selector:
    app: one8pulse
    component: sosservice
  ports:
  - port: 80
    targetPort: 8089
  type: ClusterIP
