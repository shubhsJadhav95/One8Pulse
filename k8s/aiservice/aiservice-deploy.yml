apiVersion: apps/v1
kind: Deployment
metadata:
  name: one8pulse-aiservice
  namespace: one8pulse
spec:
  replicas: 2
  selector:
    matchLabels:
      app: one8pulse
      component: aiservice
  template:
    metadata:
      labels:
        app: one8pulse
        component: aiservice
    spec:
      containers:
      - name: one8pulse-aiservice
        image: public.ecr.aws/t3q3l4n7/backend/aiservice:9
        imagePullPolicy: Always
        ports:
        - containerPort: 8083
        env:
          - name: SPRING_CONFIG_IMPORT
            value: "optional:configserver:http://one8pulse-configserver-service.one8pulse.svc.cluster.local:8888"
        envFrom:
          - secretRef:
              name: one8pulse-secrets
        resources:
          requests:
            cpu: 200m
            memory: 512Mi    # Increase from 384Mi
          limits:
            cpu: 1000m       # Increase from 500m
            memory: 1Gi      # Increase from 512Mi
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8083
          initialDelaySeconds: 15
          periodSeconds: 20
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: one8pulse-ai-service
  namespace: one8pulse
spec:
  selector:
    app: one8pulse
    component: aiservice
  ports:
  - port: 80
    targetPort: 8083
  type: ClusterIP
